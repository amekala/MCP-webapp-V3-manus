{
  "buildCommand": "npm run build",
  "devCommand": "npm start",
  "framework": "create-react-app",
  "outputDirectory": "build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/[^.]+", "dest": "/index.html" }
  ]
} 